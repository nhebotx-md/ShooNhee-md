import { financeErrorText, recordFinanceTransaction, formatRupiah } from '../../src/finance/nhefinance-commands.js'
const pluginConfig = { name: 'finance-in', alias: ['in'], category: 'finance', description: 'Catat pemasukan NHEfinance', usage: '.in 50000 Gaji | catatan', example: '.in 100000 Gaji | bonus proyek', isOwner: false, isPremium: false, isGroup: false, isPrivate: false, cooldown: 2, energi: 1, isEnabled: true }
async function handler(m) { try { const result = await recordFinanceTransaction(m, 'income'); return m.reply(`${result.duplicate ? 'ℹ️' : '✅'} *Pemasukan NHEfinance*\n\nNominal: *${formatRupiah(result.amount)}*\nKategori: ${result.category.name}\nAkun: ${result.account.name}\nCatatan: ${result.note}${result.duplicate ? '\n\nPesan ini sudah pernah dicatat; saldo tidak diubah dua kali.' : ''}`) } catch (error) { return m.reply(financeErrorText(error)) } }
export { pluginConfig as config, handler }
